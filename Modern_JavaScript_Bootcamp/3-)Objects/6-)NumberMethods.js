let numara = 103.941


console.log(numara.toFixed(2)) // içine verilen parametre kadar digitler



console.log(Math.round(numara)) // sayıyı yuvarlar
console.log(Math.floor(numara)) // sayıyı aşağı yuvarlar
console.log(Math.ceil(numara)) // sayıyı yukarı yuvarlar
